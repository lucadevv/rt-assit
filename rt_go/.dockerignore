# Local secrets — never copy into image.
.env
.env.*
!.env.example

# Build artifacts.
bin/
rtassist
coverage.out
coverage.html

# Dev-only / removed scaffolding (per current refactor).
cli/
clients/

# Editor / OS noise.
.idea/
.vscode/
.atl/
.DS_Store

# Docs not needed at runtime.
*.md

# VCS.
.git/
.gitignore
